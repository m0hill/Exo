[
  {
    "slice": 1,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Basic JSONL wire protocol with raw terminal and patch/render loop",
    "deliverables": [
      "src/lib/jsonl.zig: LineReader with safe buffer management",
      "src/lib/terminal.zig: Raw mode setup with escape hatch",
      "src/bin/backend_demo.zig: Emits patch messages over stdout",
      "src/bin/tui_runtime.zig: Polls backend, renders tree, forwards keys",
      "src/lib/render.zig: Clear screen + draw vbox/text nodes"
    ],
    "boundaries_hit": [
      "TTY input/output",
      "Child process spawn/pipe",
      "stdin polled alongside backend stdout"
    ],
    "timestamp": "2026-02-02",
    "manual_repro": [
      "zig build demo",
      "Confirm screen shows 'State: OFF'",
      "Press 'q' → screen updates to 'State: ON'",
      "Press 'q' again → 'OFF'",
      "Press 'x' → program exits, terminal restored",
      "If terminal is broken, run 'reset'"
    ]
  },
  {
    "slice": 2,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Patch-by-id plus local focus/input cursor preservation while backend keeps patching",
    "deliverables": [
      "src/lib/protocol.zig: PatchMsg v0.2 (full | target), input node, focus/input events, JSON string escaping",
      "src/bin/backend_demo.zig: Periodic clock patch-by-id + status patch on input/key events",
      "src/bin/tui_runtime.zig: Apply patch-by-id into retained tree; local input value/cursor; focus + input event emission",
      "src/lib/render.zig: Render input line and place cursor when focused"
    ],
    "boundaries_hit": [
      "Targeted patch application by node id",
      "Local interactive state preserved across patches",
      "Backend poll timeout tick loop"
    ],
    "timestamp": "2026-02-02",
    "manual_repro": [
      "zig build demo",
      "Confirm 'Tick: 0' increments every ~250ms",
      "Confirm input is focused (cursor visible on the input line); press Tab to toggle focus",
      "Type 'hello' (don't press Enter)",
      "Confirm tick keeps updating and the input stays intact (value + cursor) during patches",
      "Confirm status updates to include your last input and focus id",
      "Press 'x' (or Ctrl-C) to exit",
      "Tip: when running interactively, logs go to /tmp/tui_trace.log"
    ]
  },
  {
    "slice": 3,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Morph container subtree patches (reorder/insert/remove) while preserving local focus/input state for keyed nodes",
    "deliverables": [
      "src/lib/protocol.zig: PatchMsg v0.3 adds target patch mode (replace|morph)",
      "src/lib/tree.zig: morphPatchByIdLeaky for keyed vbox morphing + MorphStats",
      "src/bin/backend_demo.zig: Periodic mode=morph patches targeting root that reorder and sometimes insert banner",
      "src/bin/tui_runtime.zig: Apply morph patches with keyed reuse stats; drop invalid focus ids"
    ],
    "boundaries_hit": [
      "Keyed morphing of container children by id",
      "Structural updates (reorder/insert/remove) from backend while typing",
      "Patch-mode compatibility (default replace)"
    ],
    "timestamp": "2026-02-02",
    "manual_repro": [
      "zig build demo",
      "Type in the input while the backend keeps morphing the layout",
      "Observe that the input value and cursor are preserved even as nodes reorder",
      "Note that a banner sometimes appears/disappears during morph cycles (every ~6 ticks)",
      "Confirm focus remains valid (if focused node is removed, focus resets gracefully)",
      "Press 'x' to exit"
    ]
  },
  {
    "slice": 4,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Scrollable selectable list widget with local selection/scroll preserved across morph patches",
    "deliverables": [
      "src/lib/protocol.zig: list node type and select/activate events",
      "src/lib/tree.zig: morph support for list containers",
      "src/lib/render.zig: list rendering (height + scroll + selection marker)",
      "src/bin/tui_runtime.zig: focus cycling + j/k selection + enter activation; selection/scroll preservation",
      "src/bin/backend_demo.zig: continuously morph results list while updating clock/status"
    ],
    "boundaries_hit": [
      "Second stateful widget (list) with local state",
      "Keyed morphing with reorder/insert/remove under live interaction",
      "Selection + scroll stability under backend-driven structural updates"
    ],
    "timestamp": "2026-02-02",
    "manual_repro": [
      "zig build demo",
      "Tab to focus the list widget",
      "Use 'j'/'k' to move selection up/down in the list",
      "Use 'Enter' to activate an item (status shows activated item)",
      "Type in an input while the list keeps morphing; confirm selection stays on the same logical item",
      "Scroll within the list (if taller than height) and confirm scroll position is preserved",
      "Press 'x' to exit"
    ]
  },
  {
    "slice": 5,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Generic widget state registry + tree-order focus traversal for multiple inputs and lists under continuous morph patches",
    "deliverables": [
      "src/lib/render.zig: render state accepts many input/list states by id",
      "src/bin/tui_runtime.zig: widget state registry keyed by id; tab focus by tree order; per-id input/select/activate events",
      "src/bin/backend_demo.zig: multiple inputs and lists; root morph reorders panels; per-list morph patches"
    ],
    "boundaries_hit": [
      "Multiple stateful widgets with local state preserved",
      "Focus traversal derived from current tree order",
      "Backend-driven structural morphs without hardcoded widget ids"
    ],
    "timestamp": "2026-02-02",
    "manual_repro": [
      "zig build demo",
      "Confirm there are 2 inputs and 2 lists in the UI",
      "Type in both inputs; confirm each maintains its own value",
      "Tab cycles through all 4 widgets in tree order (visual top-to-bottom)",
      "Wait for root morph (every ~1 second); panels swap order",
      "Confirm Tab still follows the new visual order after reorder",
      "Confirm all input values and list selections are preserved across morphs",
      "Press 'x' to exit"
    ]
  },
  {
    "slice": 6,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Incremental renderer: render-to-frame + frame diff to emit minimal terminal updates (no full clears after first draw)",
    "deliverables": [
      "src/lib/frame.zig: Frame buffer (rows/cols, cells, row_max, cursor target)",
      "src/lib/renderer.zig: Renderer with prev/next frames + row diff + ESC[K for shrinking lines",
      "src/lib/render.zig: refactor to renderToFrame (pure frame build, no terminal I/O)",
      "src/bin/tui_runtime.zig: use Renderer.draw() instead of full-screen render per update",
      "src/test/tests.zig: incremental render test (second draw has no \\x1b[2J and much smaller output)"
    ],
    "boundaries_hit": [
      "Stateful rendering across frames (prev/next swap)",
      "O(rows*cols) diff with contiguous runs and erase-to-EOL for shrink",
      "Cursor visibility/placement preserved under partial updates"
    ],
    "timestamp": "2026-02-02",
    "manual_repro": [
      "zig build demo",
      "Confirm the UI updates without full-screen flashing (no flicker) as Tick increments (~250ms) and the list morphs",
      "Type while patches keep arriving; cursor and selection should remain stable",
      "Check /tmp/tui_trace.log for 'RENDER full=true ...' on the first draw and 'RENDER full=false ...' on subsequent draws"
    ]
  },
  {
    "slice": 7,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Resize-driven reflow: runtime redraws immediately on SIGWINCH, clamps local state, and sends debounced resize events",
    "deliverables": [
      "src/lib/protocol.zig: add resize event (rows/cols) + writeResizeEventJsonl",
      "src/bin/tui_runtime.zig: SIGWINCH self-pipe to wake poll loop; immediate redraw + debounced resize event emission",
      "src/bin/backend_demo.zig: handle resize event and patch status with rows x cols",
      "src/lib/state.zig: pure scroll clamping helper for keeping selection visible",
      "src/test/tests.zig: renderer resize repaint + cursor clamp tests; state scroll clamp test"
    ],
    "boundaries_hit": [
      "SIGWINCH delivery while poll() blocks (self-pipe wakeup)",
      "Immediate redraw on resize without waiting for backend patches",
      "Debounced resize event storm protection (~100ms)"
    ],
    "timestamp": "2026-02-02",
    "manual_repro": [
      "zig build demo",
      "Focus an input, type 'hello'",
      "Focus a list, move selection down a few items (j/k)",
      "Resize the terminal narrower/shorter while the backend is still ticking/morphing",
      "Confirm immediately:",
      "  - UI redraws to the new size (no stale screen)",
      "  - input still contains 'hello' (cursor placement remains sensible)",
      "  - list selection stays on the same item id and remains visible (scroll is clamped)",
      "  - status shows 'Size: <rows>x<cols>' (from the backend reacting to resize)",
      "Exit with 'x'"
    ]
  },
  {
    "slice": 8,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Multiline layout for text: hard newlines + soft wrap (inputs later switch to horizontal scroll in slice 9)",
    "deliverables": [
      "src/lib/render.zig: wrapped text rendering with \\n support and soft wrap",
      "src/bin/backend_demo.zig: multiline hint text (explicit newline + long wrap line)",
      "src/test/tests.zig: wrap + newline render test"
    ],
    "boundaries_hit": [
      "Renderer traversal no longer assumes one node = one line",
      "Soft-wrap behavior for text nodes"
    ],
    "timestamp": "2026-02-03",
    "manual_repro": [
      "zig build demo",
      "Make the terminal narrow (e.g. ~40 cols)",
      "Confirm the hint text respects an explicit newline and also soft-wraps the long line",
      "Note: input wrapping was superseded by slice 9 (single-line + horizontal scroll)",
      "Exit with 'x'"
    ]
  },
  {
    "slice": 9,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Input UX basics: single-line inputs with horizontal scrolling, cursor always visible, and word jumps",
    "deliverables": [
      "src/bin/tui_runtime.zig: key decoder (arrows/home/end/delete/alt-b/alt-f) + per-input scroll_x state",
      "src/lib/render.zig: input rendering as single-line viewport using scroll_x (text nodes remain multiline/wrapped)",
      "src/lib/input.zig: delete_at_cursor, ensure_cursor_visible, and word_left/word_right helpers",
      "src/test/tests.zig: viewport + cursor tests and input UX helper tests"
    ],
    "boundaries_hit": [
      "Single-line input viewport with horizontal scroll",
      "Local navigation/edit keys decoded from terminal sequences",
      "Word-jump semantics with ASCII word chars"
    ],
    "timestamp": "2026-02-03",
    "manual_repro": [
      "zig build demo",
      "Focus an input",
      "Type a long string that exceeds terminal width (e.g. 80 chars)",
      "Confirm text scrolls horizontally and the cursor stays visible",
      "Hold Left/Right: cursor moves and viewport scrolls appropriately",
      "Home/End jump to start/end (viewport updates)",
      "Backspace and Delete work (middle + edges)",
      "Alt-b / Alt-f word jumps work",
      "Backend continues ticking/morphing and input stays responsive",
      "Exit with 'x'"
    ]
  },
  {
    "slice": 10,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Rect-based layout + clipping: add hbox, padding, fixed vs flex sizing, and clipped painting within allocated rects",
    "deliverables": [
      "src/lib/protocol.zig: add hbox node + layout hints (w/h/flex) and container fields (pad/clip)",
      "src/lib/render.zig: layout→paint renderer with Rects and clipped drawing",
      "src/bin/tui_runtime.zig: focus traversal + resize/input viewport aware of laid-out widget rects",
      "src/bin/backend_demo.zig: demo tree uses hbox + padding and swaps panels horizontally under morph",
      "src/test/tests.zig: geometry/clipping unit tests"
    ],
    "boundaries_hit": [
      "Side-by-side layout requires rect-based layout pass",
      "Clipping prevents widget bleed across allocated regions",
      "Widget cursor/scroll must use per-node rect sizing"
    ],
    "timestamp": "2026-02-03",
    "manual_repro": [
      "zig build demo",
      "Confirm you see two panels side-by-side (an hbox layout)",
      "Confirm panels have padding (content is inset)",
      "Make the terminal narrow:",
      "  - long text stays clipped inside its panel (no bleed into the other column)",
      "  - focus/cursor still tracks the focused input as panels swap order",
      "Exit with 'x'"
    ]
  },
  {
    "slice": 11,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Unicode display width + simplified grapheme clusters: UTF-8 rendering, no split clusters for clipping/scroll/cursor, and wcwidth-ish column math",
    "deliverables": [
      "src/lib/unicode.zig: simplified wcwidth-ish cellWidth + nextGrapheme iterator + display width helpers",
      "src/lib/frame.zig: cell grid stores grapheme bytes + wide-char continuation markers",
      "src/lib/render.zig: grapheme-aware clipping, wrapping, and input cursor placement",
      "src/lib/input.zig: grapheme-aware backspace/delete and horizontal scrolling by byte offsets",
      "src/lib/renderer.zig: frame diff compares cells and flushes UTF-8 safely",
      "src/test/tests.zig: unit tests for combining marks, CJK width, flag clusters, and cursor math"
    ],
    "boundaries_hit": [
      "Unicode display width (0/1/2) impacts layout, clipping, and cursor placement",
      "Grapheme-safe truncation prevents split combining/ZWJ/flag sequences",
      "Frame diff no longer assumes 1 byte = 1 cell"
    ],
    "timestamp": "2026-02-03",
    "manual_repro": [
      "zig build demo",
      "Confirm the hint line shows some Unicode (e.g. 'é 漢 🇯🇵 👩‍👩‍👧‍👦') without breaking alignment",
      "Make the terminal narrow:",
      "  - the Unicode hint is clipped/soft-wrapped without splitting glyphs",
      "  - the input placeholder contains Unicode and the cursor remains aligned while typing/moving",
      "Exit with 'x'"
    ]
  },
  {
    "slice": 12,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Patch scheduler: coalesce backend patches, prioritize input/resize, and pace rendering under patch floods",
    "deliverables": [
      "src/lib/scheduler.zig: pending_full + pending_targets coalescing and bounded flushApply",
      "src/bin/tui_runtime.zig: frame-paced render loop, bounded backend drain, and resize/input priority",
      "src/bin/backend_demo.zig: --flood (10ms tick, burst=10) to generate patch floods",
      "src/test/tests.zig: scheduler coalescing + flush ordering unit tests"
    ],
    "boundaries_hit": [
      "Coalescing latest-wins patches per target",
      "Frame pacing (~30 FPS) for backend-driven updates",
      "Preventing resize debounce starvation under backend floods",
      "Bounded pending map to avoid unbounded allocations"
    ],
    "timestamp": "2026-02-03",
    "manual_repro": [
      "zig build demo -- --flood",
      "Type into an input while patches are flooding:",
      "  - input stays responsive (cursor and edits feel immediate)",
      "  - UI updates keep happening, but terminal does not feel 'busy'",
      "Resize the terminal while flood is on:",
      "  - redraw happens immediately (no stale layout)",
      "Check /tmp/tui_trace.log for:",
      "  - 'SCHED_PENDING ...' coalescing summaries",
      "  - 'RENDER reason=frame ...' bounded render cadence even under patch floods"
    ]
  },
  {
    "slice": 13,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Mouse + scroll wheel: enable SGR mouse reporting, click-to-focus/select, and local list wheel scroll with rect-based hit testing",
    "deliverables": [
      "src/lib/terminal.zig: enable/disable SGR mouse mode (1000/1006)",
      "src/lib/mouse.zig: parse SGR mouse events (left click + wheel up/down)",
      "src/bin/tui_runtime.zig: decode CSI SGR sequences, hit-test by rects, click-to-focus, click-to-select, wheel scroll",
      "src/bin/backend_demo.zig: hint text mentions mouse support",
      "src/test/tests.zig: unit tests for SGR mouse parsing"
    ],
    "boundaries_hit": [
      "Terminal mouse reporting via SGR (ESC [ < ... M)",
      "Rect-based hit testing using layout (findRectForId)",
      "Local list scroll updates without backend round trips",
      "Input-priority scheduling under backend patch floods"
    ],
    "timestamp": "2026-02-03",
    "manual_repro": [
      "zig build demo",
      "Use mouse wheel over a list: list scrolls locally (no backend interaction required)",
      "Click on a list: focus changes; click an item row: selection changes (status shows selected id)",
      "Click on an input: focus changes; typing goes into that input",
      "Run a flood: zig build demo -- --flood",
      "While flooding, click/scroll: mouse stays responsive; list scroll and focus changes apply immediately",
      "Exit with 'x' (or Ctrl-C)"
    ]
  },
  {
    "slice": 14,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Robust terminal lifecycle & crash safety: best-effort restore on panic/signal, SIGINT/SIGTERM self-pipe wakeups, and an emergency exit chord",
    "deliverables": [
      "src/lib/terminal.zig: CrashRestoreState + restoreBestEffort + emergencyExit (idempotent)",
      "src/bin/runtime/sigexit.zig: SIGINT/SIGTERM self-pipe handler and pipe drain helper",
      "src/bin/runtime/app.zig: signal pipe integrated into poll loop; restore terminal before waiting on backend",
      "src/bin/runtime/main.zig: custom panic namespace that restores terminal before default panic",
      "src/bin/demo/render.zig: hint text includes emergency exit chord"
    ],
    "boundaries_hit": [
      "Panic paths do not run defers, so terminal restore must be explicit",
      "SIGINT/SIGTERM delivery while poll() blocks (self-pipe wakeup)",
      "Idempotent restore to avoid double-cleanup edge cases",
      "Emergency exit that bypasses backend coordination"
    ],
    "timestamp": "2026-02-03",
    "manual_repro": [
      "zig build demo",
      "Press Ctrl-G, then Ctrl-G again within ~1s: program exits immediately and terminal is restored",
      "In another shell, find the tui_runtime pid and send SIGTERM (kill -TERM <pid>): program exits and terminal is restored",
      "Rerun and exit normally with 'x' or Ctrl-C: terminal is restored and /tmp/tui_trace.log includes SIGNAL_RX/EXIT (when applicable)"
    ]
  },
  {
    "slice": 15,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Styling engine v1: node-level style + inheritance, terminal capability detection, and diff-aware ANSI SGR emission",
    "deliverables": [
      "src/lib/style.zig: StyleOverride (tri-state) + merge logic + packed per-cell style",
      "src/lib/color.zig: hex + basic CSS name parsing, RGB→256/16 degradation, and env-based color mode detection",
      "src/lib/protocol.zig: optional style + placeholder_style fields (JSON parsing + validation)",
      "src/lib/frame.zig: style stored per cell + row_max considers styled blank cells",
      "src/lib/render.zig: style inheritance during paint + list selection overlay via inverse",
      "src/lib/renderer.zig: diff-aware SGR emission (only when cell style differs)",
      "src/bin/demo/render.zig: demo emits styled nodes"
    ],
    "boundaries_hit": [
      "ANSI SGR correctness across cursor moves and line erases",
      "Style-aware row_max so backgrounds render past last glyph",
      "Environment-driven mode selection (NO_COLOR, TUI_COLOR_MODE, COLORTERM, TERM)"
    ],
    "timestamp": "2026-02-03",
    "manual_repro": [
      "zig build demo",
      "Confirm panels render with background color, and titles/clock/status use colored/bold text (depending on terminal capability)",
      "Select a list row (click or j/k): selected row uses an inverse/high-contrast overlay",
      "Try forcing modes: NO_COLOR=1 zig build demo; TUI_COLOR_MODE=16 zig build demo; TUI_COLOR_MODE=256 zig build demo; TUI_COLOR_MODE=truecolor zig build demo",
      "Exit with 'x'"
    ]
  },
  {
    "slice": 16,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Styled text spans (rich text v1): inline styling within a single logical text node, with wrapping across span boundaries",
    "deliverables": [
      "src/lib/protocol.zig: styled_text node type + spans parsing",
      "src/lib/render.zig: span-aware wrapping + per-span style painting; list item support",
      "src/lib/tree.zig: morph support for styled_text nodes",
      "src/lib/scheduler.zig: clone support for styled_text nodes",
      "src/bin/runtime/ui.zig: clone/id/focus traversal supports styled_text nodes",
      "src/bin/demo/render.zig: demo includes a styled_text example line",
      "src/test/tests.zig: protocol parse + wrapping + unicode + SGR tests for spans"
    ],
    "boundaries_hit": [
      "Per-grapheme style changes without per-grapheme allocations",
      "Wrap correctness across span boundaries and explicit newlines",
      "Wide graphemes keep style across both cells"
    ],
    "timestamp": "2026-02-03",
    "manual_repro": [
      "zig build demo",
      "Confirm the 'Status: Connected (42ms) — ...' line renders with inline color/bold/dim spans",
      "Make the terminal narrow and confirm the rich line soft-wraps correctly across span boundaries",
      "Exit with 'x'"
    ]
  },
  {
    "slice": 17,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Markdown → styled_text spans (rich text v2): compile a minimal Markdown subset into existing styled_text/list/quote primitives",
    "deliverables": [
      "src/lib/markdown/mod.zig: Markdown compiler (headings, bold, italic, inline code, blockquote, bulleted lists) emitting protocol.Node trees",
      "src/lib/protocol.zig: JSON writer for nodes/spans/styles (writeNodeJson/writeSpanJson/writeStyleOverrideJson)",
      "src/bin/demo/render.zig: demo uses markdown compiler for the rich status line + a small markdown block",
      "src/test/tests.zig: golden-ish markdown compile tests + wrap-alignment render test"
    ],
    "boundaries_hit": [
      "Loss-minimizing parsing (unmatched delimiters are literal; unsupported constructs preserved as text)",
      "Prefix-layout via hbox to keep wrapped list/quote lines aligned without renderer changes",
      "Backend-side compilation only; no protocol changes required"
    ],
    "timestamp": "2026-02-03",
    "manual_repro": [
      "zig build demo",
      "Confirm the 'Status: ...' line renders bold + inline-code styling derived from Markdown",
      "Confirm a small 'Markdown demo' block shows a heading, bullet list, and blockquote prefix",
      "Make the terminal narrow: list/quote continuation lines should align under the body (hanging indent)",
      "Exit with 'x'"
    ]
  },
  {
    "slice": 18,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Streaming markdown (approach #1): backend accumulates markdown, recompiles full doc per chunk, and sends morph patches for stable incremental updates",
    "deliverables": [
      "src/bin/demo/render.zig: emitMarkdownMorphPatch compiles markdown and emits a mode=morph patch targeting a stable node id",
      "src/bin/demo/main.zig: demo streams a markdown script by appending chunks to a buffer and patching md-stream",
      "src/bin/demo/render.zig: root UI includes a fixed-height clipped md-stream container for the streaming output",
      "src/test/tests.zig: id-stability test to ensure block ids remain stable under append (enables morph reuse)"
    ],
    "boundaries_hit": [
      "Markdown prefix instability handled by full recompile (simple, robust) instead of incremental parsing",
      "Morph reuse depends on stable generated ids; appending preserves earlier ids",
      "No runtime/protocol changes required (pure backend behavior)"
    ],
    "timestamp": "2026-02-03",
    "manual_repro": [
      "zig build demo",
      "Observe the 'Streaming Markdown (Tracer 18)' panel filling in over time",
      "Make the terminal narrow: wrapped list/quote lines remain aligned under their body (hbox prefixes)",
      "Press 'x' to exit"
    ]
  },
  {
    "slice": 19,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Streaming Markdown v2: Tracer 19A (incremental finalize blocks + tail updates) and Tracer 19B (inline tail formatting), with mode/speed controls",
    "deliverables": [
      "src/lib/markdown/stream.zig: StreamBlocks (19A) + StreamInline (19B) incremental compilers with tail-only patch support",
      "src/lib/markdown/: split markdown compiler into submodules (block/inline/stream/types/common) to keep files small",
      "src/bin/demo/main.zig: demo supports Tracer 18 vs 19A vs 19B and uses leaf patches for tail updates when safe",
      "src/bin/demo/render.zig: UI adds md-mode + md-speed controls and generic emitNodeMorphPatch/emitNodePatchById helpers",
      "src/test/tests.zig: tests for 19A final-tree equivalence and 19B delimiter-closure behavior"
    ],
    "boundaries_hit": [
      "Incremental compilation without full doc reparse (19A/19B)",
      "Tail-only leaf patches to minimize payloads",
      "Stable ids across streaming to enable morph reuse",
      "Backpressure caps (max_doc_bytes/max_blocks) to bound memory"
    ],
    "timestamp": "2026-02-03",
    "manual_repro": [
      "zig build demo",
      "In md-mode, select 'Mode: Tracer 19A (finalize blocks)' and click 'Start streaming' in md-actions",
      "Confirm only the tail line changes frequently; committed blocks do not churn",
      "Switch to 'Mode: Tracer 19B (inline tail)' and start streaming; confirm **bold** / `code` formatting appears immediately once delimiters close",
      "Use md-speed controls to change cadence and chunk size; confirm streaming remains responsive",
      "Exit with 'x'"
    ]
  },
  {
    "slice": 20,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Generic scroll viewports: add a reusable scroll container with runtime-owned scroll state, wheel + keyboard scrolling, and scroll-into-view on focus changes",
    "deliverables": [
      "src/lib/protocol.zig: protocol v0.6 adds `scroll` node + optional `scroll` event",
      "src/lib/render.zig: render `scroll` with Y offset + clipping; add shared `measureContentHeight` + `findContentYRangeForId` helpers",
      "src/bin/runtime/ui.zig: local scroll state per viewport id; wheel/key scroll handlers; focus-driven scrollIntoView; emit scroll events",
      "src/bin/runtime/key_decode.zig: PageUp/PageDown decoding",
      "src/bin/demo/render.zig: wrap the streaming markdown subtree in a `scroll` viewport (md-scroll)",
      "src/bin/demo/main.zig + src/bin/demo/state.zig: track scroll events and show scroll position in the status line",
      "src/test/tests.zig: protocol + render scroll/cursor tests; ensure-visible math via `state.scrollIntoView`"
    ],
    "boundaries_hit": [
      "Protocol extension (new node + optional event) without backend state ownership",
      "Signed Y layout/rendering for viewport offsets with strict clipping (no offscreen full-buffer render)",
      "Shared measurement used for scrolling + ensure-visible logic",
      "Scroll state preserved by stable viewport ids across morph patches"
    ],
    "timestamp": "2026-02-03",
    "manual_repro": [
      "zig build demo",
      "In the 'Streaming Markdown' panel, Tab until focus reaches 'md-scroll' (or click inside the markdown output area)",
      "Use mouse wheel over the markdown output to scroll; or use 'j'/'k' (line), PgUp/PgDn (page), Home/End (jump) when the viewport is focused",
      "Confirm the status line updates with 'Scroll: md-scroll y=<n>' as you scroll",
      "Exit with 'x'"
    ]
  },
  {
    "slice": 21,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Overlays & Z-axis: add overlay layers (dropdowns/tooltips/modals) that render above normal layout, with anchor-based placement and modal input trapping",
    "deliverables": [
      "src/lib/protocol.zig: protocol v0.7 adds `overlay` node with `layers` (anchor/placement/offset/w/h/clip/modal)",
      "src/lib/render.zig: render overlay base + layered popups with clamped positioning; overlay-aware findRect + content measurement",
      "src/lib/tree.zig: patch-by-id + morph traverse and morph overlay base/layers",
      "src/bin/runtime/ui.zig: modal routing (block click-through + trap Tab focus) and topmost hit-testing for layered widgets",
      "src/bin/demo/render.zig + src/bin/demo/main.zig + src/bin/demo/state.zig: demo popups list + modal/dropdown/tooltip showcase",
      "src/test/tests.zig: overlay protocol/render/layout/findRect unit tests",
      "README.md: document protocol v0.7 + `overlay` node"
    ],
    "boundaries_hit": [
      "Absolute-positioned layers anchored to existing node ids (dropdowns/tooltips)",
      "Z-order rendering (base then layers) and overlay-aware hit-testing",
      "Modal behavior: block click-through and trap focus within modal subtree",
      "Overlay support across patch-by-id and morph patches"
    ],
    "timestamp": "2026-02-03",
    "manual_repro": [
      "zig build demo",
      "In the Popups list, activate 'Popups: open modal'",
      "Confirm modal renders above the base layout; Tab cycles focus only within modal controls",
      "Click behind the modal; confirm it does not change focus/selection",
      "Activate 'Close modal' in modal-actions to dismiss the modal",
      "Activate 'Popups: open dropdown' and confirm it appears below query-a",
      "Activate any dropdown item to close the dropdown",
      "Activate 'Popups: toggle tooltip' and confirm the tooltip follows focus changes",
      "Exit with 'x'"
    ]
  },
  {
    "slice": 22,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Box chrome: add a `box` node with borders/titles and an optional dim drop-shadow so panels + popups look intentional",
    "deliverables": [
      "src/lib/protocol.zig: protocol v0.8 adds `box` node (title/border/pad/clip/shadow + child)",
      "src/lib/render.zig: render `box` borders, inline title, and 1-cell dim shadow without affecting layout",
      "src/lib/tree.zig + src/lib/scheduler.zig: box traversal for patch-by-id/morph and deep cloning",
      "src/bin/runtime/ui.zig: box traversal for focus collection, hit-testing, modal trapping, and scroll ancestor discovery",
      "src/bin/demo/render.zig: wrap panels, dropdown, tooltip, and modal in `box` (modal becomes a dialog)",
      "src/test/tests.zig: box border/title/shadow unit tests",
      "README.md: document protocol v0.8 + `box` node"
    ],
    "boundaries_hit": [
      "Chrome rendering (border/title) integrated with layout + clipping",
      "Shadow dims existing cells without changing geometry",
      "Overlay layers render as boxed popups/dialogs"
    ],
    "timestamp": "2026-02-03",
    "manual_repro": [
      "zig build demo",
      "Confirm Panel A/B render with borders and titles in the top border",
      "Activate 'Popups: open modal' and confirm the dialog is boxed with a dim drop-shadow",
      "Activate 'Popups: open dropdown' and confirm the dropdown is boxed (and shadowed)",
      "Activate 'Popups: toggle tooltip' and confirm the tooltip is boxed (and shadowed)",
      "Exit with 'x'"
    ]
  },
  {
    "slice": 23,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Alignment settings: add flexbox-ish alignment + gaps for vbox/hbox, plus text/input alignment controls",
    "deliverables": [
      "src/lib/protocol.zig: protocol v0.9 adds alignment fields (justify_content/align_items/gap, align_self, ext_align/v_align, content_align)",
      "src/lib/render.zig: layout engine supports justify_content/align_items/gap; text ext_align/v_align; input content_align (ignored while scrolling)",
      "src/lib/tree.zig + src/lib/scheduler.zig: preserve alignment fields across morphing and deep cloning",
      "src/bin/demo/render.zig: add Alignment panel showcasing new settings",
      "README.md: document protocol v0.9 alignment fields"
    ],
    "boundaries_hit": [
      "Discrete-cell justify/spacing math across bounded/unbounded layout modes",
      "Cross-axis alignment with per-child override (align_self) without changing sizing defaults",
      "Text wrapping alignment (per visual line) and vertical centering within fixed-height rects",
      "Input rendering alignment that coexists with horizontal scrolling/cursor math"
    ],
    "timestamp": "2026-02-04",
    "manual_repro": [
      "zig build demo",
      "In the new 'Alignment' panel, confirm vbox gap spacing is visible between rows",
      "Confirm the 'justify=center' block is vertically centered within its fixed-height area",
      "Confirm the 'ext_align=right' text is right-aligned and vertically centered within its 3-row height",
      "Tab to focus the 'align-input' input and confirm its placeholder is right-aligned after the '> ' prefix",
      "In the hbox demo, confirm items A/B/C are spaced across the row (space_between) and aligned to the bottom (align_items=end)",
      "Exit with 'x'"
    ]
  },
  {
    "slice": 24,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Hover tooltip plumbing (strict opt-in): runtime emits change-only hover events and demo shows an anchored tooltip overlay",
    "deliverables": [
      "src/lib/protocol/types.zig + src/lib/protocol/parse.zig + src/lib/protocol/write.zig: protocol v0.10 adds `hoverable` node field + `hover` event (optional `item`)",
      "src/lib/terminal.zig: toggle xterm mouse motion tracking (1003) only when needed; best-effort restore disables it",
      "src/lib/mouse.zig: parse SGR mouse move events",
      "src/bin/runtime/ui/mod.zig + src/bin/runtime/app.zig: hover hit-testing (modal + overlay-aware), change-only hover emission, and pointer refresh after patches",
      "src/bin/demo/main.zig + src/bin/demo/render.zig + src/bin/demo/state.zig: demo renders a hover-driven tooltip overlay anchored to the hovered node id",
      "src/test/tests.zig: unit tests for mouse move parsing, hover protocol parsing, and list-row hover hit-testing",
      "README.md: document protocol v0.10 hover behavior"
    ],
    "boundaries_hit": [
      "Strict opt-in hover (`hoverable:true`) across all node types",
      "Mouse motion tracking enabled only when hoverables exist to reduce terminal input traffic",
      "Change-only hover event emission (no move spam) with optional list-row `item` identification",
      "Overlay/modal-aware hover hit-testing with topmost-wins semantics"
    ],
    "timestamp": "2026-02-04",
    "manual_repro": [
      "zig build demo",
      "Hover over a node marked `hoverable:true` (e.g. the panel input or results list); confirm backend logs `EVENT_RX name=hover ...` and a tooltip appears anchored to that id",
      "Move within the same hover target; confirm no additional hover events are logged (change-only)",
      "Move off any hoverable target; confirm a hover leave event is logged with `id:\"\"` and the tooltip disappears",
      "Hover over a hoverable list; confirm the hover event includes `item` and it updates as you move across rows",
      "Wheel-scroll the list under the cursor; confirm the hovered `item` updates under the same cursor position (opt-in behavior)",
      "Exit with 'x'"
    ]
  },
  {
    "slice": 25,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Mouse support v1 (full opt-in): `mouseable` nodes + unified `pointer` event with capture/double-click, SGR parsing upgrades, and dynamic terminal mouse mode gating",
    "deliverables": [
      "src/lib/protocol/types.zig + src/lib/protocol/parse.zig + src/lib/protocol/write.zig: protocol v0.11 adds `mouseable` node field + `pointer` event (down/up/move/drag/scroll)",
      "src/lib/terminal.zig: full opt-in mouse mode management (1000/1006 base + 1003 motion) with crash-safe restore",
      "src/lib/mouse.zig: robust SGR 1006 parsing (press/release/move/wheel + modifiers)",
      "src/lib/mouseable.zig: tree traversal helpers for mouseable gating + hit-test candidate collection",
      "src/bin/runtime/ui/pointer.zig + src/bin/runtime/app.zig: runtime pointer engine (hit-testing with overlays/modals/scroll offsets, capture, double-click, drag threshold, move/drag coalescing, patch-time refresh)",
      "src/bin/runtime/ui/mod.zig: runtime-local click-to-focus/select + wheel scrolling gated by `mouseable:true`",
      "src/bin/demo/render.zig + src/bin/demo/main.zig + src/bin/demo/state.zig: demo marks widgets mouseable and shows a debounced Pointer Debug status line",
      "src/test/tests.zig: mouse parsing tests, pointer protocol tests, and pointer engine click/capture/leave test",
      "README.md: document protocol v0.11 `mouseable` + `pointer` event schema"
    ],
    "boundaries_hit": [
      "Full opt-in: terminal mouse modes disabled unless `mouseable:true` or `hoverable:true` exists in the effective tree",
      "Unified pointer event stream (normalized buttons/mods/clicks + capture) to keep backend event surface extensible",
      "Overlay/modal-aware hit-testing with scroll offset correctness and best-effort coalescing under rapid motion"
    ],
    "timestamp": "2026-02-04",
    "manual_repro": [
      "zig build demo",
      "Move over mouseable widgets; confirm backend logs occasional `EVENT_RX name=pointer ...` and status shows a Pointer field (debounced for move/drag)",
      "Click a mouseable input/list to focus/select; confirm behavior matches prior slices but only on mouseable targets",
      "Wheel over a mouseable list/scroll; confirm local scrolling works and `pointer kind=scroll` events appear",
      "Remove mouseable/hoverable from the tree (if experimenting in a custom backend) and confirm terminal text selection works normally",
      "Exit with 'x'"
    ]
  },
  {
    "slice": 26,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Keyboard/Input Abstraction v1: typed key event model + robust decoder (ESC/CSI/SS3/modifiers) + bracketed paste",
    "deliverables": [
      "src/lib/keys.zig: `KeyEvent` model + modifier mask + standardized key naming helpers",
      "src/lib/key_decode.zig: state-machine decoder with ESC ambiguity timeout, CSI/SS3 decoding, and bracketed paste bodies as literal bytes",
      "src/lib/terminal.zig: enable bracketed paste (`?2004h`) on init and disable on restore/deinit (`?2004l`)",
      "src/lib/protocol/types.zig + src/lib/protocol/parse.zig + src/lib/protocol/write.zig: protocol v0.12 adds optional `mods` and `seq` to key events",
      "src/bin/runtime/app.zig + src/bin/runtime/ui/mod.zig: integrate new decoder; local widget key handling consumes `KeyEvent`; paste inserts into focused input",
      "src/test/tests.zig: decoder unit tests (ESC ambiguity, CSI timeouts, arrows+mods, F-keys, bracketed paste, UTF-8)",
      "README.md: document protocol v0.12 key naming, modifier mask, ESC timeout, and bracketed paste behavior"
    ],
    "boundaries_hit": [
      "Timeout-based ESC disambiguation for Alt-modified keys vs literal Escape",
      "No-data-loss buffering for partial CSI/SS3 sequences (unknown escapes surfaced via `seq`)",
      "Bracketed paste enabled at terminal level; paste bodies decoded as literal bytes (no key parsing inside paste)"
    ],
    "timestamp": "2026-02-04",
    "manual_repro": [
      "zig build demo",
      "Paste text containing escape sequences into the focused input (e.g. include the literal bytes of an arrow sequence like `\\x1b[A`); confirm it inserts literally and does not navigate",
      "Press Escape alone; confirm the backend logs a key event for `Escape` after a short delay",
      "In an input, use Alt-b / Alt-f and Alt-ArrowLeft / Alt-ArrowRight; confirm word-jump behavior",
      "Exit with 'x' or Ctrl-C; confirm terminal is restored (cursor visible, normal input behavior)",
      "Emergency exit chord remains: Ctrl-G then Ctrl-G"
    ]
  },
  {
    "slice": 27,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Core Widget Kit + Uniform Widget State v1: per-node disabled/readonly/validation/focusable, expanded focus traversal + action targets, state-aware styling, list marker config, and a new runtime-owned `textarea` widget",
    "deliverables": [
      "src/lib/protocol/types.zig + src/lib/protocol/parse.zig + src/lib/protocol/write.zig: protocol v0.13 adds uniform node fields `disabled`, `readonly`, `validation`, `focusable`; adds `ValidationState`, `textarea` node type, and `list.marker` (`default|none`)",
      "src/bin/runtime/ui/mod.zig + src/bin/runtime/app.zig: focus traversal includes any `focusable:true` node (action focus targets) and skips `disabled:true`; Enter/Space activates action targets via `activate` with `item:\"\"`; readonly enforced for input/textarea edits",
      "src/lib/render/mod.zig + src/lib/render/theme.zig: uniform runtime overlays for validation/disabled/readonly/focused/hovered/active; renderer support for `textarea` and list marker none",
      "src/lib/widget_kit/mod.zig + src/lib/tui.zig: backend-side widget kit (button/checkbox/toggle/radio/tabs/table/tree/textarea/progress/spinner) as compositions of protocol primitives",
      "src/bin/demo/main.zig + src/bin/demo/render.zig + src/bin/demo/state.zig: Widgets panel showcasing button/checkbox/toggle/radio/tabs/menu overlay (modal), table, tree expand/collapse, textarea (incl readonly) and validation/disabled styling",
      "src/test/tests.zig + build.zig: unit tests for protocol parse/write of new fields, textarea, list marker; render overlay behavior; textarea scroll/cursor behavior"
    ],
    "boundaries_hit": [
      "Uniform widget-state model across all nodes without adding many new protocol node types",
      "Runtime interaction gating: disabled blocks focus + pointer/hover hit-testing; readonly blocks edits for text-edit widgets only",
      "Renderer overlays driven by runtime focus/hover/active plus node validation/disabled/readonly state"
    ],
    "timestamp": "2026-02-04",
    "manual_repro": [
      "zig build demo",
      "In the Widgets panel, Tab/Shift-Tab through controls and confirm the disabled button is skipped",
      "Focus Button/Checkbox/Toggle and press Enter/Space; confirm `activate id=<id> item:\"\"` events are logged and UI updates (button click count, checkbox/toggle state)",
      "Open File/Help in the menubar; confirm a modal overlay appears and focus is trapped within it until Close menu (or pick an item)",
      "Activate tree rows (project/src/lib/tests) and confirm they expand/collapse via backend patches",
      "Edit the textarea: type multiple lines and confirm the cursor stays visible while scrolling; confirm readonly textarea blocks edits but allows cursor movement",
      "Verify validation styles are visible (error/warning/success) and disabled styling dims the disabled control",
      "Exit with 'x'"
    ]
  },
  {
    "slice": 28,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Portability + terminal integration: per-OS terminal backends (Windows VT + POSIX), termcaps negotiation (profiles + env overrides), adaptive rendering, portable IO mux, and standardized clipboard protocol/events",
    "deliverables": [
      "src/lib/termcaps.zig: termcap profiles + env overrides (`TUI_TERM_PROFILE`, `TUI_CAPS_DISABLE`), plus test-friendly detection helpers",
      "src/lib/terminal.zig + src/lib/terminal_posix.zig + src/lib/terminal_windows.zig: portable terminal facade with per-OS raw mode, VT enablement, and caps-gated feature enabling",
      "src/lib/renderer.zig: `drawWithCaps` with dumb-paint fallback when ANSI/cursor addressing is unavailable",
      "src/lib/clipboard.zig: OS clipboard (Windows API, pbcopy/pbpaste, wl-*/xclip/xsel) + OSC 52 fallback with tmux/screen passthrough",
      "src/lib/protocol/{types,parse,write}.zig: protocol v0.14 adds clipboard messages + clipboard/paste events",
      "src/bin/runtime/iomux.zig + src/bin/runtime/app.zig: portable runtime event mux (threads + queue) replacing `poll`, plus clipboard message handling and local paste insertion",
      "src/bin/demo/main.zig: portable stdin polling via `std.Io.poll` (no `std.posix.poll`)",
      "src/test/tests.zig: unit tests for clipboard protocol, OSC 52 encoding/wrappers, termcaps, and dumb renderer mode"
    ],
    "boundaries_hit": [
      "Windows-first portability baseline (VT-only) with robust state restore and clear failure messaging when VT cannot be enabled",
      "Capability-gated terminal feature use (alt screen, bracketed paste, mouse, OSC 52) to degrade gracefully across terminals",
      "Clipboard integration standardized via protocol messages/events and separated from raw input bytes/paste semantics"
    ],
    "timestamp": "2026-02-04",
    "manual_repro": [
      "POSIX: `zig build demo` and confirm behavior matches prior slices",
      "POSIX: run with `TERM=dumb zig build demo` and confirm rendering uses dumb mode (no escape spam) and remains usable",
      "Clipboard: from a backend, send `{\"type\":\"clipboard\",\"op\":\"write\",\"data\":\"hello\"}` and confirm runtime returns a clipboard ack event",
      "Clipboard: send `{\"type\":\"clipboard\",\"op\":\"read\",\"request_id\":1}` and confirm runtime returns clipboard read event + paste semantic event",
      "Windows cross-compile: `zig build -Dtarget=x86_64-windows` succeeds"
    ]
  },
  {
    "slice": 29,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Configurable keybindings: backend-driven keybinding config with context-aware resolution (global, input, textarea, list, scroll, action)",
    "deliverables": [
      "src/lib/protocol/types.zig + src/lib/protocol/parse.zig + src/lib/protocol/write.zig: protocol v0.15 adds `config` message type with `keybindings` map and `KeyAction` enum",
      "src/bin/runtime/keybindings.zig: KeymapState with per-context rule tables and default keybindings",
      "src/bin/runtime/app.zig: config message handling, key event routing through keymap resolution",
      "src/bin/runtime/ui/mod.zig: apply*Action helper functions (applyInputAction, applyTextareaAction, applyListAction, applyScrollAction, applyActionWidgetAction)",
      "src/test/keybindings.zig: unit tests for keymap resolution and config application",
      "docs/PROTOCOL.md: document config message and keybinding rules"
    ],
    "boundaries_hit": [
      "Context-first resolution (widget context → global fallback)",
      "Exact match semantics (key + mods bitmask)",
      "Replace-style config (omitted contexts keep defaults)",
      "Malformed config rejection (atomic apply)"
    ],
    "timestamp": "2026-02-08",
    "manual_repro": [
      "zig build demo",
      "Confirm Tab/Shift-Tab still cycles focus, Escape clears focus",
      "In an input: Left/Right arrows move cursor, Alt-b/Alt-f word jump, Home/End jump to line edges",
      "In a list: j/k or Up/Down move selection, Enter activates",
      "In a textarea: arrows move, Alt-b/f word jump, PgUp/PgDn page scroll, Enter inserts newline",
      "Confirm all local keybindings still work via the new keymap system",
      "Exit with 'x'"
    ]
  },
  {
    "slice": 30,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Focus groups/scopes: scoped focus traversal (trap in zone) plus explicit scope jumping actions for zone-to-zone navigation",
    "deliverables": [
      "src/lib/protocol/types.zig + src/lib/protocol/parse.zig + src/lib/protocol/write.zig: protocol v0.16 adds per-node `focus_scope` metadata (with parse alias `focus_group`) and key actions `focus_scope_next|focus_scope_prev`",
      "src/bin/runtime/ui/mod.zig: focus collection carries inherited scope; Tab/Shift-Tab traversal is trapped to current scope; new `cycleFocusScopeInTreeDir` jumps between scopes",
      "src/bin/runtime/app.zig: key action routing handles `focus_scope_next|focus_scope_prev` and emits focus events on scope jumps",
      "src/lib/tree.zig + src/lib/scheduler.zig: morph/deep-clone paths preserve `focus_scope` on all node types",
      "src/test/protocol.zig + src/test/ui.zig: tests for focus scope parse/write, `focus_group` alias, trapped cycling, and scope jumps",
      "docs/PROTOCOL.md: document `focus_scope` semantics and new key action names",
      "src/bin/demo/render.zig: focus scopes added to all UI zones (scope-left, scope-right, scope-md, scope-align, scope-widgets)",
      "src/bin/demo/main.zig: demo keybinding config sends scope jump bindings ([ -> focus_scope_prev, ] -> focus_scope_next) on startup",
      "src/bin/demo/render.zig: updated hint text mentions scoped Tab and [/] for scope jumps"
    ],
    "boundaries_hit": [
      "Inherited focus scoping from containers to descendants without introducing a new node type",
      "Transitioned to canonical `focus_scope` field only",
      "Two traversal modes sharing the same focus list: in-scope cycling vs cross-scope jumping"
    ],
    "timestamp": "2026-02-07",
    "manual_repro": [
      "zig build demo",
      "Demo includes focus scopes on all UI zones: scope-left (Panel A), scope-right (Panel B), scope-md (markdown sections), scope-align (alignment panel), scope-widgets (widgets panel)",
      "Confirm Tab/Shift-Tab cycles only within the currently focused scope",
      "Confirm `[` key triggers focus_scope_prev and `]` key triggers focus_scope_next to jump between scopes",
      "Confirm the new keys jump focus between scopes while Tab remains scoped",
      "Confirm hint text in demo UI mentions scoped Tab and [/] for scope jumps",
      "Confirm focus events continue to emit the resolved focused id after both scoped cycles and scope jumps",
      "Exit with 'x'"
    ]
  },
  {
    "slice": 31,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Grid layout container + full text editing (selection/undo/redo/copy/paste) for textarea and input",
    "deliverables": [
      "src/lib/protocol/types.zig + src/lib/protocol/parse.zig + src/lib/protocol/write.zig: protocol v0.17 adds grid node with track sizing (fixed/auto/fractional), areas, gap_x/gap_y, and grid placement fields (grid_row/grid_col/row_span/col_span/grid_area)",
      "src/lib/render/grid.zig: grid layout engine with track resolution, placement precedence (explicit > area), and cell assignment",
      "src/lib/render/mod.zig: render grid children with resolved rects and clip handling",
      "src/bin/runtime/ui/mod.zig + node_util.zig + pointer.zig: grid traversal for focus collection, hit-testing, modal detection, and node lookup",
      "src/lib/scheduler.zig + src/lib/tree.zig: grid cloning and morph support",
      "src/lib/protocol/types.zig + src/lib/protocol/parse.zig: new textarea key actions (textarea_select_left/right/up/down, textarea_select_word_left/right, textarea_select_home/end/all, textarea_copy, textarea_paste, textarea_undo, textarea_redo)",
      "src/bin/runtime/ui/mod.zig + widgets.zig: textarea selection anchor, undo/redo history (max 50 entries), copy/paste, Ctrl+Shift selection shortcuts",
      "src/lib/protocol/types.zig + src/lib/protocol/parse.zig: new input key actions (input_select_left/right, input_select_word_left/right, input_select_home/end/all, input_copy, input_paste, input_undo, input_redo)",
      "src/bin/runtime/ui/mod.zig + widgets.zig: input selection anchor, undo/redo history (max 50 entries), copy/paste, selection-aware editing",
      "src/bin/runtime/keybindings.zig: default textarea/input selection bindings (Shift+arrows, Ctrl+Shift+arrows for word select, Ctrl+A/C/V/Z/Y)",
      "src/bin/runtime/app.zig: textarea_copy/input_copy uses clipboard.writeText, textarea_paste/input_paste uses clipboard.readText",
      "src/bin/demo/render.zig: grid precedence demo node showing explicit placement overriding named areas",
      "docs/PROTOCOL.md: document grid node, track syntax, areas layout, and new textarea/input actions"
    ],
    "boundaries_hit": [
      "Grid placement precedence: explicit grid_row/grid_col overrides grid_area",
      "Textarea/input undo history bounded to 50 entries with proper memory cleanup",
      "Selection-aware editing: replace selection on type/delete/paste",
      "Clipboard integration for textarea and input via protocol-standard messages",
      "Input selection rendering coordinates with horizontal scroll viewport"
    ],
    "timestamp": "2026-02-08",
    "manual_repro": [
      "zig build demo",
      "Grid: observe 'Grid precedence (explicit row/col > area)' panel showing area:left, area:right, and explicit row=1,col=1 overriding area:left",
      "Textarea: focus the demo textarea, type some text",
      "Shift+Arrow to select text, confirm selection highlights",
      "Ctrl+C to copy selection, Ctrl+V to paste elsewhere",
      "Ctrl+Z to undo, Ctrl+Y to redo, confirm history works",
      "Ctrl+A to select all, confirm entire text selected",
      "Input: focus a single-line input, type some text",
      "Shift+Left/Right to select text within input, confirm selection highlights",
      "Ctrl+Shift+Left/Right to select by word",
      "Ctrl+C to copy selection, Ctrl+V to paste elsewhere in input",
      "Ctrl+Z to undo, Ctrl+Y to redo, confirm history works",
      "Ctrl+A to select all in input, confirm entire text selected",
      "Exit with 'x'"
    ]
  },
  {
    "slice": 32,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Controlled widget state semantics (`state_mode`) across protocol/runtime with patch-time state application for input/textarea/list/scroll",
    "deliverables": [
      "src/lib/protocol/types.zig + src/lib/protocol/parse.zig + src/lib/protocol/write.zig + src/lib/protocol/mod.zig: protocol v0.18 adds `state_mode` (`uncontrolled|init|controlled`) and per-widget state fields (`input`: value/cursor/scroll_x/selection_start/selection_end, `textarea`: value/cursor/scroll_y/selection_start/selection_end, `list`: selected_id/scroll, `scroll`: scroll_y)",
      "src/bin/runtime/ui/mod.zig + src/bin/runtime/ui/widgets.zig + src/bin/runtime/ui/node_util.zig: patch-time state sync applies backend state by mode, clamps cursor/selection/scroll, clears input/textarea undo history on backend value overwrite, and avoids event emission for patch-driven state updates",
      "src/bin/runtime/ui/mod.zig: list edge-case handling preserves uncontrolled auto-select behavior, while controlled/init modes suppress implicit auto-select",
      "src/test/protocol.zig + src/test/ui.zig + src/bin/runtime/ui_test_api.zig: roundtrip protocol coverage and runtime tests for controlled patch application + no patch-driven events + controlled list no auto-select",
      "docs/PROTOCOL.md: document controlled-state semantics, per-widget state fields, and examples",
      "src/bin/demo/render.zig: demo includes a controlled-state section (`w-controlled-input`, `w-controlled-list`, `w-controlled-scroll`) that is backend-driven every tick"
    ],
    "boundaries_hit": [
      "Dual ownership model between runtime-local interaction state and backend-authoritative state via explicit `state_mode`",
      "No synthetic event emission for patch-applied state changes to prevent feedback loops",
      "Grapheme-safe clamping for cursor/selection when backend patches text values"
    ],
    "timestamp": "2026-02-07",
    "manual_repro": [
      "zig build demo",
      "Go to the Widgets panel and find: 'Controlled state demo (state_mode=controlled)'",
      "Observe `w-controlled-input` value changing every tick (backend writes `value` + `cursor` on patch)",
      "Observe `w-controlled-list` selection flipping between items as backend patches `selected_id`",
      "Observe `w-controlled-scroll` viewport y-position changing as backend patches `scroll_y`",
      "Try interacting locally with those widgets; backend patches continue to re-assert controlled state on subsequent ticks",
      "Exit with 'x'"
    ]
  },
  {
    "slice": 33,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Design system + theming: class-based theme hooks, runtime theme switching, semantic token presets, and theme-aware chrome rendering",
    "deliverables": [
      "src/lib/protocol/types.zig + src/lib/protocol/parse.zig + src/lib/protocol/write.zig + src/lib/protocol/mod.zig: protocol v0.19 adds optional node class, theme message ({type:\"theme\",name}), and config.theme with preset names (default|light|ocean)",
      "src/lib/render/theme.zig: new Theme model with semantic tokens (surface/text/muted/border/accent/success/warn/error), per-state overlays, class rules by node type, component chrome config, and presets (default_theme, light_theme, ocean_theme)",
      "src/lib/render/mod.zig: style resolution order updated to theme class base -> state overlays -> node style override; render state now carries active theme; input/list/box chrome now theme-driven (prefix/markers/inverse/border glyphs/placeholder brackets)",
      "src/bin/runtime/app.zig: runtime applies theme updates from both config.theme and theme messages and injects active theme into render state",
      "src/lib/widget_kit/mod.zig: widgets now emit token-oriented classes; button variants (primary|secondary|danger|subtle|outline) added via buttonVariant; tabs/table/tree/radio/toggle/textarea/list nodes updated to rely on classes over ad-hoc inline styles",
      "src/lib/scheduler.zig + src/lib/tree.zig + src/bin/runtime/ui/node_util.zig: clone/morph paths preserve node class metadata",
      "src/bin/demo/main.zig: demo now supports live theme cycling (press t) by emitting protocol theme messages",
      "src/test/protocol.zig + src/test/render.zig: coverage for class parse/write roundtrip, theme/config theme parsing, and theme-dependent render differences while preserving default behavior",
      "docs/PROTOCOL.md: documented class, theme message schema, and config.theme"
    ],
    "boundaries_hit": [
      "Stable theming API uses exact class hooks (no selector engine) to keep protocol/backends simple",
      "Backward compatibility preserved: nodes without class render as before under default theme and node style still has highest precedence",
      "Theme changes are runtime-configurable without restart through both config and dedicated theme messages"
    ],
    "timestamp": "2026-02-07",
    "manual_repro": [
      "zig build demo",
      "In the demo, press t repeatedly to cycle themes (default -> light -> ocean) at runtime",
      "Confirm chrome changes with theme (input prefix, list markers, and box border style) without restarting",
      "Confirm same widget tree renders with visibly different accent/text colors across presets",
      "Confirm existing interactions still work (Tab focus, input/list editing, pointer interactions)",
      "zig build test",
      "Exit demo with x"
    ]
  },
  {
    "slice": 34,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Performance + scalability: benchmark harness, hot-path instrumentation, typed patch parse fast-path, retained id/layout caches, and runtime backpressure/seq controls",
    "deliverables": [
      "build.zig + src/test/bench.zig + docs/PERFORMANCE.md: added `zig build bench` with representative synthetic workloads (full-tree @ 30fps, target patch, huge list, heavy markdown, large patch parse) and documented target budgets",
      "src/bin/runtime/timing.zig + src/bin/runtime/app.zig + src/lib/renderer.zig: added runtime perf instrumentation (parse/scheduler/render-to-frame/diff-flush timings, bytes, changed cells) behind `TUI_PERF=1`; renderer metrics now include phase timings",
      "src/lib/protocol/parse.zig: added typed patch fast-path (`PatchWire`) to avoid full top-level dynamic map parsing on patch hot path while preserving strict validation behavior",
      "src/lib/tree.zig + src/lib/scheduler.zig: added retained `IdIndex` (`id -> path`) and indexed patch/morph application; scheduler now supports indexed flushes and tracks `max_seq_applied`",
      "src/lib/render/mod.zig + src/lib/hover.zig + src/bin/runtime/ui/pointer.zig + src/bin/runtime/ui/mod.zig + src/bin/runtime/app.zig: added retained `LayoutCache` (`id -> rect`) and wired it into hit-testing/hover/pointer/focused-rect lookups to reduce repeated traversal work",
      "src/lib/scheduler.zig + src/bin/runtime/timing.zig + src/bin/runtime/app.zig: added backpressure/coalescing knobs (`TUI_MAX_FPS`, `TUI_MAX_PENDING_TARGETS`, `TUI_MAX_BACKEND_LINES_PER_ITER`, `TUI_QUEUE_OVERFLOW=drop_newest|drop_oldest`)",
      "src/lib/protocol/types.zig + src/lib/protocol/parse.zig + src/lib/protocol/write.zig + src/lib/protocol/mod.zig + src/bin/runtime/app.zig + src/bin/demo/main.zig: added optional patch `seq`, stale-seq dropping, and optional runtime `rendered`/`dropped` telemetry events (`TUI_EMIT_RENDER_EVENTS=1`)",
      "src/test/layout.zig + src/test/tree.zig + src/test/scheduler.zig + src/test/protocol.zig: added coverage for layout cache usage, id index patching, scheduler seq/overflow behavior, and rendered/dropped + patch seq protocol parsing"
    ],
    "boundaries_hit": [
      "Kept failure behavior strict: invalid/stale lines are dropped while runtime keeps the last good tree",
      "Used optional runtime flags for observability/telemetry so default behavior remains backwards-compatible",
      "Introduced retained caches incrementally with safe fallbacks to existing traversal paths when cache paths go stale"
    ],
    "timestamp": "2026-02-07",
    "manual_repro": [
      "zig build test",
      "zig build bench -- --iters 20",
      "Confirm bench output prints all 5 workloads with avg_ms/target_ms/fps",
      "Run runtime with perf logs: `TUI_PERF=1 zig build demo` and confirm `PERF parse_ns=... sched_flush_ns=... render_to_frame_ns=... diff_flush_ns=...` entries in runtime log",
      "Run runtime with sequencing telemetry enabled: `TUI_EMIT_RENDER_EVENTS=1 zig build demo` and confirm backend log shows `EVENT_RX name=rendered ...`",
      "Optionally set backpressure knobs and verify startup `RUNTIME_CFG` log reflects values (e.g. `TUI_MAX_FPS=60 TUI_MAX_PENDING_TARGETS=64 TUI_QUEUE_OVERFLOW=drop_oldest`)"
    ]
  },
  {
    "slice": 35,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Unicode text metrics: grapheme-aware layout with configurable width/ tab handling and context-aware rendering",
    "deliverables": [
      "src/lib/unicode.zig: TextMetrics struct with configurable Options (ambiguous_width, tab_width, fast_approx)",
      "src/lib/unicode.zig: context-aware width calculations via graphemeWidthAtCol with column-dependent tab expansion",
      "src/lib/unicode.zig: WrapIter for grapheme-aware text wrapping with row/col tracking",
      "src/lib/unicode.zig: proper handling of \\r (ignored) and \\t (expanded to tab stops)",
      "src/lib/render/mod.zig: all rendering paths updated to use TextMetrics for consistent width calculations",
      "src/lib/render/text.zig: span and text rendering with grapheme-safe clipping and selection",
      "README.md: added Unicode/Text Guarantees section documenting grapheme awareness and width behavior",
      "docs/DEVELOPMENT.md: added Unicode knobs documentation (TUI_UNICODE_AMBIGUOUS_WIDTH, TUI_TAB_WIDTH, TUI_UNICODE_FAST_APPROX)",
      "src/test/unicode.zig + src/test/render.zig: unit tests for TextMetrics, wrap iterator, and rendering with new unicode handling"
    ],
    "boundaries_hit": [
      "Terminal-style Unicode width with emoji ZWJ/flags/modifiers and variation selectors",
      "Ambiguous East Asian width configurable (narrow default vs wide)",
      "Tab stops are context-aware (expand based on current column position)",
      "CR (\\\\r) is ignored for layout/editing (no carriage return behavior)",
      "Out of scope: full bidi reordering/shaping and terminal-font-specific glyph shaping"
    ],
    "timestamp": "2026-02-08",
    "manual_repro": [
      "zig build test",
      "zig build demo",
      "Type text with tabs in an input/textarea; confirm tabs expand to 4-column stops by default",
      "Type text with \\r characters; confirm they are ignored (no visual effect)",
      "Type emoji sequences (flags, ZWJ like 👩‍👩‍👧‍👦, modifiers); confirm proper width calculation and rendering",
      "Test with TUI_TAB_WIDTH=8 zig build demo; confirm tab stops change",
      "Test with TUI_UNICODE_AMBIGUOUS_WIDTH=wide zig build demo; confirm ambiguous chars (e.g., some CJK punctuation) render as 2 cells",
      "Exit with 'x'"
    ]
  },
  {
    "slice": 36,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Protocol versioning: optional version field on all message types with compatibility policy",
    "deliverables": [
      "src/lib/protocol/types.zig: added optional `v` (?u32) field to all message types (PatchMsg, EventMsg, ClipboardMsg, ConfigMsg, ThemeMsg, PointerEvent, etc.)",
      "src/lib/protocol/parse.zig: require `v` field on all messages and reject unknown fields",
      "src/lib/protocol/write.zig: write optional `v` field for all message types; version-conditional field emission to minimize payload size",
      "src/lib/protocol/mod.zig: added PROTOCOL_VERSION constant (1) and convenience helpers",
      "docs/PROTOCOL.md: documented strict version semantics (`v` required and fixed) and current version",
      "src/bin/demo/render.zig: demo emits `v:1` on all patch messages via DEMO_PROTOCOL_VERSION constant",
      "src/bin/demo/main.zig: demo updated to use protocol version",
      "src/test/protocol.zig: roundtrip tests for version field on all message types"
    ],
    "boundaries_hit": [
      "Receivers require `v` on every message and reject unsupported versions",
      "Writers always emit `v` to keep wire format explicit and canonical",
      "Unknown fields are rejected to match strict canonical schema",
      "Removing or changing meaning of existing fields requires new version rollout plan"
    ],
    "timestamp": "2026-02-08",
    "manual_repro": [
      "zig build test",
      "zig build demo",
      "Observe backend log shows `v:1` on all outgoing patch messages",
      "Confirm runtime accepts and processes messages both with and without `v` field",
      "Verify docs/PROTOCOL.md describes version compatibility policy",
      "Exit with 'x'"
    ]
  },
  {
    "slice": 37,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Runtime startup handshake: emit `hello` event with protocol version, detected caps, and runtime limits",
    "deliverables": [
      "src/lib/protocol/types.zig: added `EventMsg.hello` plus typed `HelloEvent`, `HelloCaps`, and `HelloLimits` payload structs",
      "src/lib/protocol/parse.zig: added parse support for `{\"type\":\"event\",\"name\":\"hello\",...}` with required `protocol_version`, `caps`, and `limits` fields",
      "src/lib/protocol/write.zig + src/lib/protocol/mod.zig: added `writeHelloEventJsonl(...)` helpers and exported `PROTOCOL_VERSION` constant",
      "src/bin/runtime/app.zig: emits `hello` immediately after backend spawn/pipe setup (before first render), including terminal caps and active runtime limits",
      "docs/PROTOCOL.md: documented `hello` startup handshake payload and semantics",
      "src/test/protocol.zig: added write+parse test coverage for `hello` event payload"
    ],
    "boundaries_hit": [
      "Handshake is sent once at runtime startup to avoid backend env guessing",
      "Caps payload reflects detected terminal/runtime capabilities (`ansi`, `alt_screen`, `bracketed_paste`, `mouse_sgr`, `osc52`, `color`)",
      "Limits payload reflects active scheduler/backpressure knobs (`max_fps`, `frame_interval_ns`, `max_pending_targets`, `max_backend_lines_per_iter`, `queue_overflow`)"
    ],
    "timestamp": "2026-02-08",
    "manual_repro": [
      "zig build test",
      "zig build demo",
      "Observe first runtime->backend event is `name=hello` with protocol version + caps + limits",
      "Confirm backend can branch on `hello.caps` and `hello.limits` without reading env vars",
      "Exit with 'x'"
    ]
  },
  {
    "slice": 38,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Structured runtime errors: machine-readable `event:error` for invalid lines, invalid patch shape/mode, and config rejection",
    "deliverables": [
      "src/lib/protocol/types.zig: added `EventMsg.error` (`@\"error\"`) with `RuntimeErrorEvent { code, message, seq?, context?, v? }`",
      "src/lib/protocol/parse.zig + src/lib/protocol/write.zig + src/lib/protocol/mod.zig: added parse/write/export support for `{\"type\":\"event\",\"name\":\"error\",...}`",
      "src/bin/runtime/app.zig: mapped parse/config log-only failures to runtime `event:error` emission with a rate limiter (200ms window)",
      "docs/PROTOCOL.md: documented `event:error` schema and stable error codes (`invalid_line`, `invalid_patch_shape`, `config_rejected`)",
      "src/test/protocol.zig: added write+parse coverage for `event:error`"
    ],
    "boundaries_hit": [
      "Error events are rate-limited to prevent floods under repeated invalid backend lines",
      "Runtime still logs locally; `event:error` augments logs with machine-readable backend-facing diagnostics",
      "Kept payload additive and version-compatible using optional fields (`seq`, `context`, `v`)"
    ],
    "timestamp": "2026-02-08",
    "manual_repro": [
      "zig build test",
      "Run runtime/backend and send malformed JSON line from backend; confirm runtime emits `event:error code=invalid_line`",
      "Send patch with invalid shape or unknown mode; confirm runtime emits `event:error code=invalid_patch_shape`",
      "Send invalid config keybinding/theme; confirm runtime emits `event:error code=config_rejected` with context",
      "Send repeated invalid lines quickly; confirm error events are rate-limited (logs show `EVENT_DROP name=error ... reason=rate_limit`)"
    ]
  },
  {
    "slice": 39,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Config acknowledgement events: runtime emits `config_ack` in response to backend config messages with applied/rejected breakdown",
    "deliverables": [
      "src/lib/protocol/types.zig: added `ConfigAckEvent` and `ConfigAckRejected` structs with `applied`, `rejected` fields",
      "src/lib/protocol/types.zig: added `EventMsg.config_ack` variant",
      "src/lib/protocol/write.zig: added `writeConfigAckEventJsonl` helper for emitting config_ack events",
      "src/lib/protocol/parse.zig: added parse support for config_ack events",
      "src/bin/runtime/app.zig: emits config_ack after processing config message; maps parse errors to rejected entries",
      "src/bin/runtime/app.zig: helper functions for building config rejection entries and config hint parsing",
      "docs/PROTOCOL.md: documented config_ack event schema, applied/rejected rules, and keybindings-first evaluation behavior",
      "src/test/keybindings.zig: added tests for config_ack emission on reject, theme cascading reject when keybindings fail",
      "src/test/protocol.zig: added parse/write roundtrip tests for config_ack events"
    ],
    "boundaries_hit": [
      "One config_ack per config message attempt",
      "applied array lists accepted top-level keys (keybindings, theme)",
      "rejected array lists failed keys with reason strings",
      "Keybindings evaluated first; if keybindings fail, theme is rejected with reason 'keybindings_rejected'",
      "Malformed config uses key 'config' for rejection"
    ],
    "timestamp": "2026-02-08",
    "manual_repro": [
      "zig build test",
      "zig build demo",
      "Send valid config from backend; confirm runtime emits config_ack with applied=['keybindings','theme'], rejected=[]",
      "Send invalid keybindings config; confirm config_ack shows rejected=[{key:'keybindings',reason:'...'}]",
      "Send config with both keybindings and theme where keybindings are invalid; confirm both are rejected with theme reason='keybindings_rejected'",
      "Exit with 'x'"
    ]
  },
  {
    "slice": 40,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Sequencing acknowledgements: runtime emits `ack` events for backend messages with `seq`, enabling backpressure and delivery confirmation",
    "deliverables": [
      "src/lib/protocol/types.zig: added `AckEvent` struct with `seq`, `status`, `detail` fields; added `EventMsg.ack` variant",
      "src/lib/protocol/types.zig: added `seq` field to `ClipboardMsg` (write/read) and `ConfigMsg`",
      "src/lib/protocol/write.zig: added `writeAckEventJsonl` helper for emitting ack events",
      "src/lib/protocol/parse.zig: added parse support for ack events and seq fields on clipboard/config messages",
      "src/lib/scheduler.zig: `FlushResult` now includes `applied_seqs` and `not_found_seqs` arrays for tracking sequenced operations",
      "src/lib/scheduler.zig: added seq tracking in `putTargetLeakyWithSeq`, `putFullLeakyWithSeq` and flush result collection",
      "src/bin/runtime/app.zig: emits ack events for queued/coalesced/applied/dropped operations with appropriate status and detail",
      "docs/PROTOCOL.md: documented ack event schema, status values (queued/coalesced/applied/dropped_*), and backpressure guidance",
      "src/test/scheduler.zig: added tests for seq tracking in flush results and dropped_overflow ack payload",
      "src/test/protocol.zig: added parse/write roundtrip tests for ack events"
    ],
    "boundaries_hit": [
      "One ack per backend message that includes seq",
      "Status values: queued, coalesced, applied, dropped_overflow, dropped_stale, dropped_no_root, dropped_not_found, ignored_invalid",
      "Detail field provides runtime context (full/target/queue_overflow/stale_seq/target_not_found)",
      "Ack ordering is monotonic per stdout stream; queued may arrive before terminal status",
      "Backends should bound in-flight seq messages to max_pending_targets"
    ],
    "timestamp": "2026-02-08",
    "manual_repro": [
      "zig build test",
      "zig build demo",
      "Send patch with seq; confirm runtime emits ack with status=queued, then later status=applied",
      "Send target patch with seq for non-existent id; confirm ack status=dropped_not_found with detail=target_not_found",
      "Send patches rapidly to overflow queue; confirm ack status=dropped_overflow with detail=queue_overflow",
      "Send config with seq; confirm ack status=applied after processing",
      "Exit with 'x'"
    ]
  },
  {
    "slice": 41,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Protocol cleanup: remove theme message, consolidate into config; strict schema validation; remove legacy aliases",
    "deliverables": [
      "src/lib/protocol/types.zig: removed `ThemeMsg` type; theme now only via `ConfigMsg.theme`",
      "src/lib/protocol/parse.zig: removed `theme` message parsing; added `UnknownField` error for strict validation",
      "src/lib/protocol/write.zig: removed `writeThemeMsgJsonl` and theme message write support",
      "docs/PROTOCOL.md: documented `protocol.schema.json` as canonical artifact; updated strictness policy (`additionalProperties: false`)",
      "docs/PROTOCOL.md: removed `theme` message documentation; theme only via config",
      "docs/PROTOCOL.md: removed `focus_group` alias documentation (now only `focus_scope`)",
      "docs/PROTOCOL.md: documented list no longer auto-selects on patch (backend must set `selected_id`)",
      "AGENTS.md: added protocol schema validation section with `scripts/check_protocol_schema.py` usage",
      "README.md: added `protocol.schema.json` and `SCHEMA_DRIFT.md` references",
      "src/bin/demo/main.zig: removed theme message emission; use config for theme changes",
      "src/lib/terminal_posix.zig + src/lib/terminal_windows.zig: removed unused code",
      "src/test/protocol.zig: updated tests for removed theme message and stricter validation"
    ],
    "boundaries_hit": [
      "Theme changes consolidated into config message (single channel for runtime configuration)",
      "Strict schema validation: unknown fields rejected, not ignored (`additionalProperties: false`)",
      "Removed compatibility aliases to simplify protocol surface",
      "Canonical schema in `protocol.schema.json` is source of truth for SDKs and validation"
    ],
    "timestamp": "2026-02-08",
    "manual_repro": [
      "zig build test",
      "python3 scripts/check_protocol_schema.py --docs docs/PROTOCOL.md",
      "zig build demo",
      "Confirm theme changes still work via config (press 't' to cycle themes)",
      "Confirm `focus_group` no longer parsed (use `focus_scope` instead)",
      "Exit with 'x'"
    ]
  },
  {
    "slice": 42,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Mouse-driven text interaction: editable cursor placement/drag-select, document-style read-only selection overlay, and commit-on-release selection events",
    "deliverables": [
      "src/bin/runtime/ui/text_selection.zig: runtime document selection engine with nearest scroll scoping, local overlay state, selection extraction, truncation, and commit dedupe",
      "src/lib/renderer.zig: added screen-selection overlay support (`ScreenSelection`) applied post-render with inverse attribute",
      "src/bin/runtime/ui/mod.zig: mouse cursor placement for input/textarea, drag selection capture state (`EditDragState`), shift-extend support, and wrapped textarea hit mapping",
      "src/bin/runtime/app.zig: wired document selection consume path before pointer events, commit-on-mouse-up `event:selection`, Ctrl-C local copy preference for document selection, and renderer overlay integration",
      "src/lib/protocol/types.zig: added `SelectionEvent` + `SelectionKind` and `EventMsg.selection` variant",
      "src/lib/protocol/write.zig + src/lib/protocol/parse.zig + src/lib/protocol/mod.zig: selection event writer/parser/export",
      "protocol.schema.json + docs/PROTOCOL.md: documented `event:selection` fields and truncation rules",
      "src/test/protocol.zig + src/test/renderer.zig + src/test/ui.zig: added roundtrip, overlay, and mouse interaction tests",
      "src/bin/demo/main.zig: handles `event:selection` and surfaces selected text metadata in demo status"
    ],
    "boundaries_hit": [
      "Runtime-only high-frequency selection rendering during drag (no per-move protocol traffic)",
      "Single backend notification on mouse-up with capped payload (`16384` bytes) and truncation flag",
      "Document selection scoped to nearest scroll viewport when available",
      "Ctrl-C copies document selection locally before backend key forwarding",
      "Input/textarea mouse behavior aligns with existing local state and emits input updates through existing paths"
    ],
    "timestamp": "2026-02-11",
    "manual_repro": [
      "zig build test",
      "zig build demo",
      "Click in a mouseable input/textarea to place cursor at pointer location (not forced to end)",
      "Drag in input/textarea to create local selection; release and continue typing to confirm state updates",
      "Drag across mouseable text/styled_text inside a scroll viewport; confirm local inverse highlight during drag",
      "Release mouse button; confirm demo logs/patches a single `event:selection` with text/bytes/truncated fields",
      "Press Ctrl-C with an active document selection; confirm local clipboard copy behavior",
      "Exit with 'x'"
    ]
  },
  {
    "slice": 43,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Protocol-level virtualization for big collections: new `vlist` node, runtime `vlist_range` requests, deterministic item ids, and index-aware item events",
    "deliverables": [
      "src/lib/protocol/{types,parse,write,mod}.zig + protocol.schema.json: added `vlist` node, `event:vlist_range`, and optional `index`/`item_index` on item-bearing events",
      "src/lib/render/mod.zig: added `RenderState.vlists`, `VListState`, and `paintVList` rendering (loaded window rows + unloaded placeholder rows)",
      "src/bin/runtime/ui/{widgets,mod,node_util}.zig: added vlist widget state (`scroll`, `selected_index`, request tracking), sync/apply logic, key/mouse behavior, and range request emission helpers",
      "src/bin/runtime/app.zig: requests vlist ranges after patch sync and passes vlist render state",
      "src/lib/hover.zig + src/bin/runtime/ui/pointer.zig: vlist hit testing with deterministic items and global indices (`hover.index`, `pointer.item_index`)",
      "src/test/{protocol,ui,render}.zig + src/bin/runtime/ui_test_api.zig: coverage for vlist parse/write, vlist range requests/coalescing, vlist select index payloads, and vlist rendering behavior",
      "docs/PROTOCOL.md + docs/PERFORMANCE.md: documented `vlist`, `vlist_range`, deterministic id contract, and virtualization workflow",
      "src/bin/demo/{render,main}.zig: added a virtual list demo panel (`results-vlist`) and backend handling for `event:vlist_range` window patches"
    ],
    "boundaries_hit": [
      "Runtime owns local vlist scroll/selection and requests backend windows on demand",
      "Backend window identity is deterministic (`item_id_prefix + index`) and index is carried on item-bearing events",
      "Range requests are suppressed when current backend window already covers visible+overscan rows"
    ],
    "timestamp": "2026-02-11",
    "manual_repro": [
      "zig build test",
      "python3 scripts/check_protocol_schema.py --docs docs/PROTOCOL.md",
      "zig build demo",
      "In demo, focus panel \"Virtual List (1e6 rows)\" and scroll/select; confirm `EVENT_RX name=vlist_range ...` and targeted vlist window patches",
      "Inspect emitted select/activate events for vlist and confirm `item` + `index` are present",
      "Exit with 'x'"
    ]
  },
  {
    "slice": 44,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Rich selector-based theming with pseudo-states, theme variables, and runtime config.theme_spec",
    "deliverables": [
      "src/lib/render/theme_engine.zig: selector parser/matcher, state flags, compiled-rule cascade with specificity+order, and rule buckets",
      "src/lib/render/theme.zig + src/lib/render/mod.zig: theme vars, dynamic OwnedTheme builder from ThemeSpec, and selector-engine integration in paint pipeline",
      "src/lib/protocol/{types,parse,write,mod}.zig + protocol.schema.json: StyleOverride supports $vars, ConfigMsg.theme_spec types/parse/write, strict theme_spec validation + limits, and new parse errors",
      "src/bin/runtime/app.zig: config.theme_spec apply/swap with owned theme lifecycle, config_ack handling for theme_spec, and parse-error rejection mapping",
      "src/bin/demo/main.zig: demo keybinding (y) to send runtime theme_spec updates",
      "src/test/{protocol,style,render,keybindings}.zig: coverage for $var parse/write, theme_spec parse/write, selector matching/cascade, pseudo-state rendering, missing var fallback, and config reject behavior",
      "docs/PROTOCOL.md: documented selector grammar, class token semantics, $var colors, and config.theme_spec"
    ],
    "boundaries_hit": [
      "Selector v1 constrained to a single compound selector (no combinators/grouping)",
      "Theme vars resolve at render-time; missing vars degrade to inherit with rate-limited THEME_VAR_MISS logging",
      "theme_spec size protections enforced (rules/vars/name/selector limits)"
    ],
    "timestamp": "2026-02-11",
    "manual_repro": [
      "zig build test",
      "python3 scripts/check_protocol_schema.py --docs docs/PROTOCOL.md",
      "zig build demo",
      "In demo: press t to cycle base theme; press y to toggle dynamic theme_spec and observe hover/focus styling changes",
      "Send backend config with theme_spec vars/rules and confirm runtime emits config_ack applied includes theme_spec",
      "Verify $missing var in styles does not crash; runtime logs THEME_VAR_MISS and style inherits"
    ]
  },
  {
    "slice": 45,
    "name": "tracer-slice",
    "status": "completed",
    "description": "Scroll affordances: always-visible vertical scrollbars (when scrollable) with wheel tuning, track click jump, and thumb drag suppression",
    "deliverables": [
      "src/lib/protocol/{types,parse,write,mod}.zig + protocol.schema.json: added optional `config.scrolling` with strict parse/write and range checks",
      "src/lib/render/theme.zig: added scrollbar chrome glyphs (`scrollbar_track_glyph`, `scrollbar_thumb_glyph`) and builtin rules (`scrollbar.track`, `scrollbar.thumb`)",
      "src/lib/render/mod.zig: added shared `computeScrollbar(...)`, scrollbar painting, and gutter-aware rendering for `scroll`, `list`, and `textarea`",
      "src/bin/runtime/ui/scrollbar.zig: added scrollbar drag/click geometry/state utilities",
      "src/bin/runtime/ui/mod.zig: added wheel tuning + Shift-page wheel behavior, scrollbar track click jump, thumb drag capture, and mouse suppression result path",
      "src/bin/runtime/app.zig: threaded scrolling config defaults + `config.scrolling` apply/ack/reject, wired scrollbar drag state, and suppressed pointer emission during scrollbar interactions",
      "src/test/{protocol,render,ui,pointer,keybindings}.zig: added coverage for scrolling config roundtrip, scrollbar rendering, wheel/click/drag behavior, and pointer suppression",
      "docs/PROTOCOL.md + src/bin/demo/render.zig: documented `config.scrolling` and updated demo interaction hints"
    ],
    "boundaries_hit": [
      "Scrollbar gutter reserves the rightmost content column only when the widget is scrollable and width permits",
      "Wheel behavior is runtime-tunable and modifier-aware (`Shift` page step) while preserving existing local-vs-backend state semantics",
      "Scrollbar drag/click is runtime-local and suppresses backend pointer events for the gesture sequence"
    ],
    "timestamp": "2026-02-13",
    "manual_repro": [
      "zig build test",
      "python3 scripts/check_protocol_schema.py --docs docs/PROTOCOL.md",
      "zig build demo",
      "Hover a mouseable scroll/list/textarea with overflow: confirm right-side scrollbar is visible",
      "Wheel over mouseable textarea/list/scroll: confirm local scrolling (Shift+wheel pages)",
      "Click scrollbar track: confirm jump scroll; drag thumb: confirm continuous scroll and no pointer event spam",
      "Send config `{ \"type\":\"config\", \"v\":1, \"scrolling\":{...} }`: confirm `event:config_ack` includes `scrolling` in applied/rejected",
      "Exit with 'x'"
    ]
  },
  {
    "slice": 46,
    "name": "tracer-slice",
    "status": "completed",
    "description": "More complete layout constraints: node min/max and percent sizing, bounded flex redistribution, and single-line text ellipsis overflow",
    "deliverables": [
      "src/lib/protocol/{types,parse,write,mod}.zig + protocol.schema.json: added optional node layout hints (`min_w/max_w/min_h/max_h/w_pct/h_pct`) across all node types and `overflow` (`clip|ellipsis`) on `text`/`styled_text`",
      "docs/PROTOCOL.md: documented new layout fields, precedence (`w/h` > `%` > auto), min/max clamp semantics, and single-line overflow behavior",
      "src/lib/render/mod.zig: centralized hint/min/max helpers; added bounded `hbox` redistribution with min/max caps and deterministic overflow truncation; added vbox pinned-height (%/fixed) handling and max-size rect clamping in paint/rect lookup paths",
      "src/lib/render/text.zig: added grapheme-safe ellipsis fitting helpers for single-line rendering",
      "src/lib/tree.zig + src/lib/scheduler.zig + src/bin/runtime/ui/node_util.zig: propagated new fields through morph/clone paths so constraints/overflow survive runtime patching",
      "src/bin/demo/render.zig: added a constraints demo section with `w_pct` sidebar, flex panels with min/max bounds, and list rows using ellipsis overflow",
      "src/test/protocol.zig + src/test/render.zig: added protocol roundtrip coverage for new fields and render tests for percent splits, constrained flex redistribution, vbox percent height pinning, and ellipsis rendering"
    ],
    "boundaries_hit": [
      "Percent sizing is parent-relative and clamped by node min/max hints",
      "HBox allocation now redistributes growth/shrink under constraints and falls back deterministically when total mins overrun available width",
      "Ellipsis applies only in single-line list/vlist row rendering; wrapped text rendering remains unchanged",
      "Track-level grid `minmax(...)` remains deferred; node-level min/max works inside existing `fixed|auto|fr` tracks"
    ],
    "timestamp": "2026-02-13",
    "manual_repro": [
      "zig build test",
      "python3 scripts/check_protocol_schema.py --docs docs/PROTOCOL.md",
      "zig build demo",
      "In demo, inspect the constraints row: sidebar uses `w_pct=30` and main panels redistribute width while respecting min/max",
      "Make terminal narrower/wider and confirm panel widths clamp predictably",
      "Check list rows in the constraints sidebar and confirm long text ends with ellipsis instead of hard clipping",
      "Exit with 'x'"
    ]
  }
]
